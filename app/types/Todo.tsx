// Define the structure of a Todo item using TypeScript
export interface Todo {
    id: number;        // Unique identifier
    text: string;      // Task description
    completed: boolean; // Whether the task is completed or not
  }
  